export const XBOX_API_KEY = '7bf89c48-8174-4f34-bdd4-716cc07fe5f7'
export const MY_XBOX_USER_ID = '2535430809538288'
export const LIES_OF_P_ID = '2071061510'
export const GIANT_BOMB_API_KEY = '7832659a0f9760b77a1355f155cf78bd55c4220b'
export const RAWG_API_KEY = '383b1d7c0b384606833b84b585899abf'

/* const arr = [2, 4, 43, 2]
const bubleSort = (arr) => {
  for (let i = 0; i < arr.length; i++) {
    for (let j = 0; j < i; j++) {
      if (arr[j] > arr[j + 1]) {
        const aux = arr[j]
        arr[j] = arr[j + 1]
        arr[j + 1] = aux
        console.log(`${arr[j + 1]} > ${arr[j]}`)
      } else {
        console.log(`${arr[j]} < ${arr[j + 1]}`)
      }
    }
    console.log('-------------------------------')
  }
  return arr
}

const otherBubleSort = (arr) => {
  let count = 0
  for (let i = arr.length - 1; i >= 0; i--) {
    for (let j = 0; j < i; j++) {
      if (arr[j] > arr[j + 1]) {
        const aux = arr[j]
        arr[j] = arr[j + 1]
        arr[j + 1] = aux
        console.log(`${arr[j + 1]} > ${arr[j]}`)
      } else {
        console.log(`${arr[j]} < ${arr[j + 1]}`)
      }
      count++
    }
    console.log('-------------------------------')
  }
  console.log(count)
  return arr
}

console.log(otherBubleSort(arr))
*/
